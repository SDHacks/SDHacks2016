//- Homepage

extends layout

block content
  .hero
    svg
      defs
        pattern(id="logo", width="1", height="1", viewBox="0 0 100 100", preserveAspectRatio="xMidYMid")
          circle(cx="50%", cy="50%", r="100%", class="hero__hexagon--bg")
          image(xlink:href="/assets/img/logo.png", width="100", height="100", preserveAspectRatio="xMidYMid")
        pattern(id="logo-filled", width="1", height="1", viewBox="0 0 100 100", preserveAspectRatio="xMidYMid")
          circle(cx="50%", cy="50%", r="100%", class="hero__hexagon--bg-filled")
          image(xlink:href="/assets/img/logo.png", width="100", height="100", preserveAspectRatio="xMidYMid")
    svg.js-background-mesh.hero__background-mesh(height="150px")
  div.container.home-container
    div.row
      h1 Hello <span class="mean">{{guest.name}}</span>
      p  
        if user === null
          a(href="/login") Login
        else
          a(href="/profile") #{user.name}
      hr
      h3 Project structure
      ol.tips
        li /
        | Root of the project. Contains <b>server.js</b>, <b>package.json</b> and <b>bower.json</b> among other important configuration files
        hr
        li /entities
        | Inside here, the scaffold generator creates a folder for each <b>entity</b> in your project. This entity folder contains the files <b>model.coffee</b> and <b>controller.coffee</b> for such entity
        hr
        li /extras
        | Miscelaneous <b>configuration files</b>, <b>middleware</b> and/or <b>custom modules</b> (passport, oatuh, etc.)
        hr
        li /log
        | All logs go here. No more explanations needed
        hr
        li /static
        | Your <b>assets</b> and <b>Angular SPA</b> code reside here
        hr
        li /routes
        | Your project's <b>routes</b> are declared here
        hr
        li /scripts
        | Folder for <b>random scripts</b> you might need to include (<b>DevOps tasks</b> for example)
        hr
        li /views
        | The <b>views</b> for your entities and your project overall... This folder includes <b>layouts</b> and <b>partials</b>

block scripts
  script(src='/assets/bower/bower_components/jquery-throttle-debounce/jquery.ba-throttle-debounce.min.js')
  script(src='/assets/js/hexagons.js')